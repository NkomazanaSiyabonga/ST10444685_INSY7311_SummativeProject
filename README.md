
  # Student Intranet UI Mockup

  This is a code bundle for Student Intranet UI Mockup. The original project is available at https://www.figma.com/design/Lq9k6WoyxlLVWjiF98D93L/Student-Intranet-UI-Mockup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # summative-project-NkomazanaSiyabonga
